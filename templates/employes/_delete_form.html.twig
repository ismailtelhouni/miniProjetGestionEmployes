{#<form method="post" action="{{ path('app_employes_delete', {'id': employe.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ employe.id) }}">
    <button class="btn"><img class='moussa' src='{{ asset('assets/img/remove.png') }}'>Delete</button>
</form>#}

<form id='Delete' method='post' action="{{ path('app_employes_delete', {'id': employe.id}) }}" onsubmit="return confirm('étes-vous sur de vouloir supprimer ce donnée ?');" >
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ employe.id) }}">
    <button class='delete' type='submit' name='submit1' style='color:red;'>
    <i class="fa-solid fa-user-xmark" style='color:red;'></i> </button>
</form>